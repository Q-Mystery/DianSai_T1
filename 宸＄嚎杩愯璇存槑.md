# 黑线白底巡线：接线、编译和运行说明

## 1. 固件功能

本工程用于 MSPM0G3507 两轮小车在白色地面上跟随黑线：

- 八路灰度模块使用 `AD2/AD1/AD0` 选通 0～7 通道，经 `OUT` 读取数字电平；每次切换通道后等待 `EIGHT_IR_CHANNEL_SETTLE_US`。
- 上电后不再等待白底标定或放线提示，初始化完成后直接进入巡线；默认按白底输出高电平、黑线输出低电平归一化为 `BK=1`。
- 巡线轮速由八路加权误差连续计算，不再按 X3/X6、X2/X7、X1/X8 固定档位跳变；误差越大，基础速度越低，左右轮差速由 PD 输出连续调节。
- 中央区域默认以 185 mm/s 巡线，中等偏移基础速度 145 mm/s，强偏移基础速度 95 mm/s；最大纠偏差速为 125 mm/s，纠偏时单轮最低保留 20 mm/s；丢线后先以 20 mm/s 寻线，连续丢线超过18次扫描即停车，全程不允许任何车轮反转。
- E1 为左轮编码器，E4 为右轮编码器；20 ms 更新一次闭环速度。
- MD310 参数按 20:1 减速比、13 线编码器、A/B 两相上升沿计数配置为 520 脉冲/轮圈；48 mm 轮径对应轮周长 150.796 mm。
- 目标速度默认不超过 260 mm/s，PID 输出 PWM 封顶为 340，并叠加 160 的逻辑 PWM 起步死区；编码器主动超速刹车默认关闭，确认编码器倍率后可在 `APP/app_control_config.h` 中开启 `MOTOR_OVERSPEED_BRAKE_ENABLE`。

## 2. 确定的引脚分配

左轮使用驱动板 A 通道，右轮使用 D 通道。

| 功能 | 外设信号 | MSPM0G3507 引脚 | BoosterPack 针脚 | 说明 |
|---|---|---|---|---|
| 左轮调速 | PWMA | PB12 / TIMA0.CCP1 | 19 | 图中 PWM，约 40 kHz |
| 左轮方向 | AIN1 | PB15 | 17 | GPIO 输出 |
| 左轮方向 | AIN2 | PB17 | 18 | GPIO 输出 |
| 右轮调速 | PWMD | PB4 / TIMA0.CCP2 | 40 | 图中 PWM，约 40 kHz |
| 右轮方向 | DIN1 | PA12 | 32 | GPIO 输出 |
| 右轮方向 | DIN2 | PA13 | 31 | GPIO 输出 |
| 左编码器 A 相 | E1A | PB8 | 15 | 上升沿中断 |
| 左编码器 B 相 | E1B | PB7 | 14 | 上升沿中断 |
| 右编码器 A 相 | E4A | PB20 | 36 | 上升沿中断 |
| 右编码器 B 相 | E4B | PB13 | 35 | 上升沿中断 |
| 灰度地址位 2 | AD2 | PB0 | 12 | GPIO 输出 |
| 灰度地址位 1 | AD1 | PB6 | 13 | GPIO 输出 |
| 灰度地址位 0 | AD0 | PB16 | 11 | GPIO 输出 |
| 灰度数字结果 | OUT | PB1 | 39 | 上拉输入 |

其他已配置接口：OLED 软件 I²C 改为图中专用排针 PB2(SCL/9)、PB3(SDA/10)，支持 SSD1306 128×32 的 `0x3C/0x3D` 地址自动检测；调试串口 UART0 为 PA10(TX/34)、PA11(RX/33)、115200-8-N-1。为释放排针，未使用的 LED 与蜂鸣器 GPIO 已从巡线构建中取消。电机与灰度模块均不依赖 UART/I²C 通信。

本分配避开 PA5/PA6 外部晶振、PA19/PA20 SWD、NRST、J14/J15 决定的歧义针脚 3 和 29，以及所有重复占用。

## 3. 断电接线

1. 断开电池、USB 和调试器电源，架空两个车轮。
2. 按上表连接 10 根电机控制/编码器线和 4 根灰度接口线。
3. 左电机接驱动板 A 通道输出，右电机接 D 通道输出。
4. 若驱动板有 `STBY/EN`，必须按该板原理图可靠置为使能状态。
5. 编码器按其规格接 3.3 V 和 GND；不要把 5 V 编码器输出直接接入 MSPM0 GPIO。
6. 灰度模块接其规定电源、GND、AD2、AD1、AD0、OUT。首次上电前用万用表确认 OUT 高电平不超过 MSPM0 输入允许值。
7. 电机使用符合 MD310 规格的独立电源；MSPM0、驱动板、编码器、灰度模块和电机电源负极必须共地。
8. 不要用开发板的小电流 5 V 接口直接给两个电机供电。

## 4. CCS 编译

在 Windows 工程根目录打开 PowerShell，执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\ccs\build_ccs_theia.ps1
```

成功后生成：

- `Debug/eight_ir_oled_new.out`
- `Debug/eight_ir_oled_new.hex`
- `Debug/eight_ir_oled_new.map`

也可在 CCS Theia 导入工程根目录后 Build Project；烧录时优先加载 `.out`，单独使用通用烧录器时可选择 `.hex`。

在 macOS/Linux 可直接执行：

```bash
./ccs/build_ti_clang.sh
```

## 5. 上电直接巡线

1. 上电或复位前先把小车放到赛道上，使中央 X4/X5 位于黑线上。
2. 固件完成初始化后立即进入巡线主循环，不显示 `CAL: WHITE` 或 `PLACE LINE`，也不再等待 1 s/2 s 启动延时。
3. OLED 状态页会按主循环刷新，显示运行时间、速度、编码器、障碍距离、语音状态和 IMU 状态。
4. 当前直接启动模式默认白底原始电平为高、黑线原始电平为低；若实测相反，需要恢复白底标定流程或调整 `BSP/bsp_ir_eight.c` 中的默认 `g_ir_white_level`/`g_ir_black_level`。

## 6. 方向和编码器检查

首次实车运行前必须架空车轮，用 CCS 调试器分别调用或临时单步执行 `PWM_Control_Car(200, 0)` 和 `PWM_Control_Car(0, 200)`：

1. 正命令应使对应车轮驱动车体向前。当前实车已确认 `motor_D` 需要反转，因此 `APP/app_control_config.h` 中 `RIGHT_MOTOR_DIR_REVERSED` 默认为 1；若后续更换电机或接线，再按实际方向调整该配置。
2. 在 Watch 窗口观察 `motorL_encoder.count` 和 `motorR_encoder.count`；车轮向前时应为正。若相反，把 `APP/app_control_config.h` 中对应的 `ENCODER_E1_REVERSED` / `ENCODER_E4_REVERSED` 改为 1。
3. 黑线从物理左侧探头移到右侧时，OLED 的 `BK` 有效位应从 X1 依次移到 X8。若顺序相反，把 `APP/app_control_config.h` 的 `EIGHT_IR_SENSOR_REVERSED` 改为 1。
4. 每次修改后重新编译和烧录，确认左右轮及编码器均一致后再把车放到地面。

## 7. 正式巡线与调参

1. 使用宽阔、低速、无坠落风险的封闭赛道，并准备随时切断电机电源。
2. 每次复位前先把车放到赛道起点；复位后小车会直接开始巡线。
3. 若直线左右振荡，先减小 `APP/app_control_config.h` 的 `LINE_TURN_KP`；转向迟钝则小幅增大。
4. 入弯冲过头可适量增大 `LINE_TURN_KD`；高频抖动则减小 KD。
5. 如果仍然冲过头，先降低 `LINE_BASE_SPEED_MM_S` 和 `LINE_MAX_TURN_DELTA_MM_S`，再检查传感器高度、黑线宽度和左右顺序。
6. 所有生效的巡线、电机速度 PID、编码器、速度和保护参数集中在 `APP/app_control_config.h`；电机速度 PID 当前为 `Kp=0.30`、`Ki=0.010`、`Kd=0`。巡线采用八路加权误差连续 PD 差速，直线 `185 mm/s`，中偏 `145 mm/s`，强偏 `95 mm/s`。

## 8. 仍需由实物确认的信息

代码和参考工程无法替代以下实物信息，正式落地前必须确认：

- 驱动板准确型号、`STBY/EN` 的接法以及 PWM/方向输入是否为高电平有效。
- 八路灰度模块和编码器的实际供电电压、OUT/A/B 输出高电平电压是否兼容 3.3 V GPIO。
- X1 是否确实安装在车体左侧；左右电机线序和编码器正方向是否与代码一致。
- 实际 MD310 是否为 20:1、13 线、48 mm 轮径版本；若不是，需要按铭牌修改脉冲数和轮周长。
